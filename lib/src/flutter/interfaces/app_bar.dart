part of 'interfaces.dart';

/// AppBar abstrait : étend Relement pour s’insérer partout.
/// Implémentations attendues : titre, leading, actions…
abstract class AppBarI extends Relement {
  AppBarI({super.id});
}
